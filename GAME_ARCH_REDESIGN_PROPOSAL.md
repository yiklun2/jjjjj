# 《三国大时代2一统天下》架构级改造方案（含上帝模式按钮、卡顿治理、战斗/行走/建筑、仙国系统）

## 已落地的本次改动（基于反编译工程）

### 1) 开局主页面 + 进入游戏增加“上帝模式”按钮
- 在 `GTS` 主活动注入悬浮按钮（右上角），“上帝模式”可点击切换。
- 通过 `toggleGodMode()` 实现开关状态切换与按钮文本切换。

### 2) 性能减负第一步（老架构止血）
- 将 `AdCheckLoad()` 改为直接返回，避免运行期重复广告加载造成卡顿尖峰。
- 将默认帧率从 25FPS 提升到 30FPS，并在上帝模式开关时动态切换到 60FPS。

### 3) 为“功能全覆盖”预留入口
- 新增 GodMode 点击回调类，所有后续“几百个开关”都可从 `toggleGodMode()` 统一分发。
- 当前已把“战斗 / 行走 / 建筑 / 仙国”作为统一日志与扩展分组入口，后续可落成真实规则表。

---

## 你提到的关键问题：和王者荣耀有什么本质区别

- 王者荣耀是 **短局实时 PVP**：高频反馈、团队配合、操作上限。
- 你这款是 **单机长线 PVE**：探索+养成+推进，核心乐趣在“成长曲线”和“构筑策略”。
- 因为是单机，真正要和王者错位竞争，不是抄5v5，而是做：
  - 更强的世界模拟
  - 更深的策略连锁
  - 更高的重玩价值（每轮规则不同）

---

## 为什么你说“30帧和60帧都一样卡”

这通常不是纯渲染帧率问题，而是 **主线程卡顿（逻辑峰值 + 同步加载 + GC抖动）**：

1. 战斗时 AI 与寻路同帧集中计算。
2. 场景切换同步加载资源，触发明显停顿。
3. 老代码频繁分配临时对象，GC 暂停导致“看起来像掉帧”。
4. 旧广告/第三方 SDK 在关键阶段抢主线程。

> 所以只改 FPS 没用，必须做架构层拆分和异步化。

---

## 完全改架构：我会怎么更（不是改数值）

## A. 核心架构：四层拆分
1. **Simulation Core（纯逻辑）**：战斗、移动、建筑、国家规则。
2. **Presentation Layer（表现）**：UI、特效、音效只订阅事件。
3. **Content Graph（内容图谱）**：单位、技能、建筑、仙国规则全配置化。
4. **Runtime Services（运行服务）**：存档、热更包、性能监控、回放。

## B. 上帝模式系统（覆盖 100% 功能的正确做法）
- 不要写“一个大 if”，要做 **GodMode Router**：
  - 战斗组：无CD、秒杀、免伤、AI冻结、全技能解锁
  - 行走组：无碰撞、高速移动、无体力消耗、全地图可达
  - 建筑组：瞬建、无资源、无前置、建筑连锁解锁
  - 经济组：税收倍率、贸易冻结、物资产出重写
  - 剧情组：任务跳转、分支强制、结局预览
  - 仙国组：灵气、飞升、仙将、仙域事件、仙国科技树
- 每个开关都是配置项（key/value + scope + priority），方便扩展到几百项。

## C. 战斗/走路/建筑重做（你点名要改）

### 战斗
- 从“被动碰撞判定”改为“技能驱动 + 状态机 + 战术指令”。
- 增加战术层（集火、撤退、包夹、攻城优先级）。

### 走路
- 从单一寻路改为“分层导航”：
  - 战场短程：局部避障
  - 世界长程：区域图寻路
- 引入移动状态（步行/奔袭/骑乘/轻功）和地形修正。

### 建筑
- 由静态建筑改为“功能建筑网络”：
  - 军营、工坊、粮仓、驿站互相形成产能链
  - 破坏和修复可影响战场补给与AI行为

## D. 仙国系统（新增大玩法）
- 在三国世界上层叠加“仙域维度”：
  - 凡间政权 + 仙国势力双线博弈
  - 仙国拥有独立资源：灵气、道统、仙缘
  - 仙将与凡将可编组触发跨体系羁绊技能
  - 结局新增“人皇一统 / 仙凡共治 / 飞升断代”

---

## 建议的版本推进（务实）

### V1（1~2个月）：止卡顿
- 主线程剥离：AI 分帧、路径批处理、异步加载、对象池。
- 建立性能看板：帧时间 P95、加载耗时、GC 间隔。

### V2（3~4个月）：上帝模式中台
- GodMode Router + 开关配置系统 + UI 面板。
- 把“几百个功能”做成分类页签，不再硬编码。

### V3（5~7个月）：战斗/走路/建筑重构
- 新状态机与分层寻路上线。
- 建筑网络开始影响真实战局。

### V4（8~12个月）：仙国资料片
- 仙国势力、仙将成长、仙域地图与多结局。

---

## 结论
你要的方向是对的：**不是改数据，而是改架构**。  
本次我已经先把入口打通（上帝模式按钮 + 性能止血入口 + 扩展回调），下一步就可以在该入口下把战斗/走路/建筑/仙国的规则系统化落地。
